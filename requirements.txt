pydantic>=2.6
python-dotenv>=1.0
requests>=2.31

# Embedding / reranking
# B1 (coding_plan.md): >=3.1 khuyến nghị nếu dùng EMBEDDING_USE_MRL_TRUNCATE=true
# (Qwen3-Embedding MRL truncate_dim). embed.py đã tự degrade êm (try/except)
# về encode thường nếu bản cài đặt < 3.1, nên KHÔNG bump cứng ở đây để tránh
# phá môi trường đang chạy model cũ (AITeamVN/Vietnamese_Embedding không cần
# tính năng này).
sentence-transformers>=3.0
torch>=2.2

# Generation (HF transformers in-process). TextIteratorStreamer (B3
# streaming) ships with transformers itself — no extra package needed.
transformers>=4.44
accelerate>=0.30

# Retrieval
rank_bm25>=0.2.2
tiktoken>=0.7

# Vector store
pinecone>=5.0

# Prompt compression — STILL UNUSED (see backend/generation/compress.py's
# docstring). Kept installed for the same reason it was kept in the ALQAC
# era: cheap to keep, documents a real legal-domain constraint. Drop this
# line if compress.py is ever deleted.
llmlingua>=0.2.2

# Ingestion of supplementary law documents
python-docx>=1.1
pdfplumber>=0.11

# --- A2 (coding_plan.md): HTTP server wrapper cho chat_pipeline.py -------
fastapi>=0.110
uvicorn>=0.29

# --- D3 (coding_plan.md): judge client (test/judge_client.py) dùng requests
# thuần (đã có ở trên) để gọi Anthropic API trực tiếp — KHÔNG thêm SDK/
# LangChain, giữ tinh thần no-framework của core pipeline.
